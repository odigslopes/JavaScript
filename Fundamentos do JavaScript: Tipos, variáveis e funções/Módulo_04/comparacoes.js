//  == { comparação implícita }

const numero = 5;
const texto = "5";

console.log(numero === texto);

// typeof => tipo de dado salvo na variável
console.log(typeof numero);
console.log(typeof texto);

// == { compara somente o valor }
// === { compara o valor e o tipo de dado da variável }

// Conversão explícita

Number();
String();